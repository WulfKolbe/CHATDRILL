which can, for instance, be achieved with SGD. 
To distribute the scaling factor on the different layers, we use again the parameter transformation in Lemma (ref), which is {{2110.11150_FO0116||FO}}
which ensures that the overall output of the neural network is scaled by {{2110.11150_FO0117||FO}}.