As a general remark, note that the scaling factor of the output {{2110.11150_FO0050||FO}} quickly approaches zero for increasing depth, which could render one-shot pruning numerically infeasible. 
For that reason, we propose a computationally cheap rescaling procedure that allows us to find significantly sparser strong lottery tickets.
This is also helpful for maintaining the trainability of the pruned network {{2110.11150_REF_rescaleInit||CIT}}, which we discuss next in the context of initializations with nonzero biases.